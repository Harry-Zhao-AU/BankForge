---
phase: 02.1
slug: ci-cd-foundation
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-04-15
---

# Phase 02.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | JUnit 5 (Jupiter) via `spring-boot-starter-test` |
| **Config file** | No `junit-platform.properties` — default discovery |
| **Quick run command** | `mvn -B test -pl account-service` |
| **Full suite command** | `mvn -B test -pl account-service,payment-service,ledger-service,notification-service` |
| **Estimated runtime** | ~60–120 seconds (Testcontainers startup per service) |

---

## Sampling Rate

- **After every task commit:** Run `mvn -B test -pl account-service` (fastest — no Kafka Testcontainers)
- **After every plan wave:** Run full suite (all 4 services) or observe CI run
- **Before `/gsd-verify-work`:** All matrix jobs green in GitHub Actions
- **Max feedback latency:** ~120 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 02.1-01-01 | 01 | 0 | CI-01 | — | N/A | file-check | `test -f .github/workflows/ci.yml` | ❌ Wave 0 | ⬜ pending |
| 02.1-01-02 | 01 | 0 | CI-02 | — | N/A | file-check | `test -f account-service/Dockerfile && test -f payment-service/Dockerfile && test -f ledger-service/Dockerfile && test -f notification-service/Dockerfile` | ❌ Wave 0 | ⬜ pending |
| 02.1-01-03 | 01 | 1 | CI-03 | T-02.1-01 | DOCKER_HOST not exposed to non-Windows env | grep | `! grep -r "DOCKER_HOST" account-service/pom.xml payment-service/pom.xml ledger-service/pom.xml notification-service/pom.xml 2>/dev/null \| grep -v "<profile>"` | ✅ after fix | ⬜ pending |
| 02.1-01-04 | 01 | 1 | CI-06 | — | N/A | integration | `mvn -B test -pl account-service,payment-service,ledger-service,notification-service` | ✅ existing | ⬜ pending |
| 02.1-02-01 | 02 | 2 | CI-04 | T-02.1-02 | GITHUB_TOKEN scoped to packages:write only | manual | Observe Actions tab — matrix jobs run on push to main | N/A (runtime) | ⬜ pending |
| 02.1-02-02 | 02 | 2 | CI-05 | T-02.1-03 | Image tag includes immutable SHA; :latest is floating | manual | `docker pull ghcr.io/<owner>/bankforge-account-service:latest` | N/A (runtime) | ⬜ pending |
| 02.1-02-03 | 02 | 2 | CI-07 | — | N/A | manual | Observe PR check run — no image push step executes | N/A (runtime) | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `.github/workflows/ci.yml` — create new (prerequisite job + matrix jobs)
- [ ] `account-service/Dockerfile` — create new (multi-stage JDK21 build + JRE21 runtime)
- [ ] `payment-service/Dockerfile` — create new
- [ ] `ledger-service/Dockerfile` — create new
- [ ] `notification-service/Dockerfile` — create new
- [ ] All 4 service `pom.xml` files — move `DOCKER_HOST` + `TESTCONTAINERS_RYUK_DISABLED` from base Surefire config into OS-conditional `<profile><activation><os><family>Windows</family></os></activation></profile>`

*These are the primary deliverables of Phase 02.1. Wave 0 IS the phase output.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Matrix jobs run in parallel on push to main | CI-04 | Requires GitHub Actions runtime | Push a commit to main; observe Actions tab — 4 service jobs should start simultaneously after prerequisite job completes |
| Images appear in ghcr.io with correct tags | CI-05 | Requires GitHub Actions runtime + package registry | After main push CI completes: `docker pull ghcr.io/<owner>/bankforge-account-service:latest` and `docker pull ghcr.io/<owner>/bankforge-account-service:sha-<short-sha>` |
| PRs run build+test only (no push) | CI-07 | Requires GitHub Actions runtime | Open a PR; observe that the image push step is skipped or only runs on main |
| Tests pass on ubuntu-latest | CI-06 | Requires GitHub Actions runtime (DOCKER_HOST fix verified locally first) | Observe CI run — all 4 service jobs green |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 120s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
