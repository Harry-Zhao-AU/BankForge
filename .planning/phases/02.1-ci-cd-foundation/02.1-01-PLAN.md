---
phase: 02.1-ci-cd-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - account-service/pom.xml
  - payment-service/pom.xml
  - ledger-service/pom.xml
  - notification-service/pom.xml
autonomous: true
requirements:
  - CI-03
  - CI-06

must_haves:
  truths:
    - "mvn test on Linux auto-detects Docker at /var/run/docker.sock without DOCKER_HOST override"
    - "mvn test on Windows still uses npipe:////./pipe/docker_engine via the podman-windows profile"
    - "All 4 service POMs retain the <includes> block for *Test.java and *IT.java under Surefire"
  artifacts:
    - path: "account-service/pom.xml"
      provides: "Surefire config with OS-conditional DOCKER_HOST profile"
      contains: "<family>Windows</family>"
    - path: "payment-service/pom.xml"
      provides: "Surefire config with OS-conditional DOCKER_HOST profile"
      contains: "<family>Windows</family>"
    - path: "ledger-service/pom.xml"
      provides: "Surefire config with OS-conditional DOCKER_HOST profile"
      contains: "<family>Windows</family>"
    - path: "notification-service/pom.xml"
      provides: "Surefire config with OS-conditional DOCKER_HOST profile"
      contains: "<family>Windows</family>"
  key_links:
    - from: "*/pom.xml base Surefire config"
      to: "*/pom.xml <profiles> section"
      via: "Maven profile merge — profile <configuration> merges with base <configuration>"
      pattern: "podman-windows"
---

<objective>
Fix DOCKER_HOST Surefire configuration in all 4 service POMs so Testcontainers works on both Windows (local dev with Podman named pipe) and Linux (GitHub Actions CI with Docker socket auto-detection).

Purpose: Without this fix, `mvn test` on ubuntu-latest will fail because `DOCKER_HOST=npipe:////./pipe/docker_engine` points Testcontainers at a non-existent Windows named pipe. This is a prerequisite for CI to function at all.

Output: 4 modified pom.xml files with DOCKER_HOST and TESTCONTAINERS_RYUK_DISABLED moved into a `podman-windows` Maven profile activated only on Windows OS.
</objective>

<execution_context>
@C:/Users/CORP+Harry.Zhao/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/CORP+Harry.Zhao/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.1-ci-cd-foundation/02.1-CONTEXT.md
@.planning/phases/02.1-ci-cd-foundation/02.1-RESEARCH.md

<interfaces>
<!-- All 4 service POMs have identical Surefire configuration that needs the same fix. -->
<!-- The current (broken for CI) structure in each service pom.xml: -->

Current base Surefire config (same in all 4):
```xml
<plugin>
  <groupId>org.apache.maven.plugins</groupId>
  <artifactId>maven-surefire-plugin</artifactId>
  <configuration>
    <includes>
      <include>**/*Test.java</include>
      <include>**/*IT.java</include>
    </includes>
    <environmentVariables>
      <DOCKER_HOST>npipe:////./pipe/docker_engine</DOCKER_HOST>
      <TESTCONTAINERS_RYUK_DISABLED>true</TESTCONTAINERS_RYUK_DISABLED>
    </environmentVariables>
  </configuration>
</plugin>
```

Target state (same in all 4):
```xml
<!-- Base config: includes only, NO environmentVariables -->
<plugin>
  <groupId>org.apache.maven.plugins</groupId>
  <artifactId>maven-surefire-plugin</artifactId>
  <configuration>
    <includes>
      <include>**/*Test.java</include>
      <include>**/*IT.java</include>
    </includes>
  </configuration>
</plugin>

<!-- In <profiles> section: -->
<profiles>
  <profile>
    <id>podman-windows</id>
    <activation>
      <os>
        <family>Windows</family>
      </os>
    </activation>
    <build>
      <plugins>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-surefire-plugin</artifactId>
          <configuration>
            <environmentVariables>
              <DOCKER_HOST>npipe:////./pipe/docker_engine</DOCKER_HOST>
              <TESTCONTAINERS_RYUK_DISABLED>true</TESTCONTAINERS_RYUK_DISABLED>
            </environmentVariables>
          </configuration>
        </plugin>
      </plugins>
    </build>
  </profile>
</profiles>
```

Maven profile merge behavior (VERIFIED): The profile's `<configuration>` merges with the base plugin `<configuration>`. The `<includes>` from the base config are preserved when the Windows profile activates and adds `<environmentVariables>`. This is standard Maven behavior documented at maven.apache.org.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move DOCKER_HOST and TESTCONTAINERS_RYUK_DISABLED into OS-conditional Maven profile in all 4 service POMs</name>
  <files>account-service/pom.xml, payment-service/pom.xml, ledger-service/pom.xml, notification-service/pom.xml</files>
  <read_first>
    - account-service/pom.xml (current Surefire config to modify)
    - payment-service/pom.xml (current Surefire config to modify)
    - ledger-service/pom.xml (current Surefire config to modify)
    - notification-service/pom.xml (current Surefire config to modify)
    - .planning/phases/02.1-ci-cd-foundation/02.1-RESEARCH.md (Pattern 4: Surefire OS-Conditional Profile — exact XML to produce)
  </read_first>
  <action>
For each of the 4 service pom.xml files (account-service, payment-service, ledger-service, notification-service), make these exact changes:

1. **Remove `<environmentVariables>` from the base Surefire `<configuration>`:**
   - Delete the entire `<environmentVariables>` block (containing `<DOCKER_HOST>` and `<TESTCONTAINERS_RYUK_DISABLED>`) from inside `<plugin>maven-surefire-plugin</plugin> > <configuration>`.
   - Keep the `<includes>` block exactly as-is (`**/*Test.java` and `**/*IT.java`).
   - Remove the XML comment about "Surefire configuration for Testcontainers + Podman on Windows" that precedes the plugin declaration (the comment is now misleading since the base config is OS-neutral).

2. **Add a `<profiles>` section at the end of the `<project>` element** (after `</build>`, before `</project>`):

```xml
    <profiles>
        <profile>
            <id>podman-windows</id>
            <activation>
                <os>
                    <family>Windows</family>
                </os>
            </activation>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-surefire-plugin</artifactId>
                        <configuration>
                            <environmentVariables>
                                <DOCKER_HOST>npipe:////./pipe/docker_engine</DOCKER_HOST>
                                <TESTCONTAINERS_RYUK_DISABLED>true</TESTCONTAINERS_RYUK_DISABLED>
                            </environmentVariables>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>
```

3. **Update the Surefire plugin comment** to reflect the new structure. Replace the old Windows-specific comment block with:
```xml
            <!--
                Surefire includes both *Test.java and *IT.java (integration tests).
                DOCKER_HOST for Podman on Windows is set via the podman-windows profile below.
                On Linux/CI, Testcontainers auto-detects Docker at /var/run/docker.sock.
            -->
```

The result for each POM: the base Surefire plugin has `<includes>` only (no env vars), and a `podman-windows` profile at `</build>` level adds the Windows-specific env vars when Maven detects a Windows OS.

Per D-05: Tests still run identically on Windows (profile auto-activates). On ubuntu-latest, DOCKER_HOST is not set, so Testcontainers auto-detects Docker.
  </action>
  <verify>
    <automated>cd /c/SideProject/BankForge && for svc in account-service payment-service ledger-service notification-service; do echo "=== $svc ===" && grep -c "podman-windows" "$svc/pom.xml" && grep -c "<family>Windows</family>" "$svc/pom.xml" && echo "Base env vars (should be 0):" && grep -A2 "maven-surefire-plugin" "$svc/pom.xml" | grep -c "DOCKER_HOST" | head -1; done</automated>
  </verify>
  <acceptance_criteria>
    - Each of the 4 service pom.xml files contains exactly one `<profile>` with `<id>podman-windows</id>`
    - Each pom.xml contains `<family>Windows</family>` inside the profile activation
    - The base `<plugin>maven-surefire-plugin` `<configuration>` in each POM contains `<includes>` but does NOT contain `<environmentVariables>` or `<DOCKER_HOST>`
    - `grep -c "podman-windows" account-service/pom.xml` returns 1
    - `grep -c "podman-windows" payment-service/pom.xml` returns 1
    - `grep -c "podman-windows" ledger-service/pom.xml` returns 1
    - `grep -c "podman-windows" notification-service/pom.xml` returns 1
    - The `<includes>` block in each POM still contains `**/*Test.java` and `**/*IT.java`
    - `DOCKER_HOST` and `TESTCONTAINERS_RYUK_DISABLED` appear ONLY inside the `<profiles><profile>` block, not in the base Surefire config
  </acceptance_criteria>
  <done>All 4 service pom.xml files have DOCKER_HOST moved into a podman-windows Maven profile with Windows OS activation. The base Surefire config contains includes only. Local Windows `mvn test` still works (profile auto-activates). Linux CI `mvn test` will use Docker socket auto-detection.</done>
</task>

<task type="auto">
  <name>Task 2: Verify tests still pass locally on Windows with the profile-based configuration</name>
  <files>account-service/pom.xml, payment-service/pom.xml, ledger-service/pom.xml, notification-service/pom.xml</files>
  <read_first>
    - account-service/pom.xml (verify profile is present after Task 1)
  </read_first>
  <action>
Run the full test suite locally to confirm that the Maven profile auto-activation works correctly on Windows. The `podman-windows` profile should activate automatically (because we are on Windows) and set DOCKER_HOST to the Podman named pipe.

Run: `"C:/Program Files/JetBrains/IntelliJ IDEA Community Edition 2025.2.4/plugins/maven/lib/maven3/bin/mvn" -B test -pl account-service,payment-service,ledger-service,notification-service` with `JAVA_HOME="C:/Program Files/JetBrains/IntelliJ IDEA Community Edition 2025.2.4/jbr"`

This validates that:
1. The `podman-windows` profile activates on Windows OS
2. DOCKER_HOST is correctly passed to Surefire via the profile
3. Testcontainers can still connect to the Podman named pipe
4. All *Test.java and *IT.java tests still execute (the includes block was preserved)

If any test fails, check: (a) is the `<includes>` block still present in the base config? (b) does the profile XML merge correctly? The profile `<configuration>` should merge with (not replace) the base Surefire `<configuration>`.

Note: This requires Podman machine to be running. If Podman is not running, start it with `podman machine start` first.
  </action>
  <verify>
    <automated>cd /c/SideProject/BankForge && JAVA_HOME="C:/Program Files/JetBrains/IntelliJ IDEA Community Edition 2025.2.4/jbr" "C:/Program Files/JetBrains/IntelliJ IDEA Community Edition 2025.2.4/plugins/maven/lib/maven3/bin/mvn" -B test -pl account-service 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `mvn -B test -pl account-service` exits with BUILD SUCCESS
    - Test output shows both *Test.java and *IT.java classes executed (not 0 tests run)
    - No "Could not find a valid Docker environment" errors in output
    - The Maven build log shows `podman-windows` profile is active (visible in Maven debug output or inferred from Testcontainers connecting successfully)
  </acceptance_criteria>
  <done>Local Windows test suite passes with the profile-based Surefire configuration, confirming zero regression from the DOCKER_HOST refactor. CI can now proceed without DOCKER_HOST override on Linux.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer machine -> Maven build | Surefire env vars are build-time config, not runtime secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-02.1-01 | Information Disclosure | pom.xml DOCKER_HOST | mitigate | DOCKER_HOST value (npipe path) moved into OS-conditional profile; not exposed to Linux/CI environments where it has no meaning and could confuse Testcontainers auto-detection |
</threat_model>

<verification>
1. All 4 service POMs contain `podman-windows` profile with `<family>Windows</family>` activation
2. No POM has `DOCKER_HOST` or `TESTCONTAINERS_RYUK_DISABLED` outside the profile block
3. `<includes>` block preserved in base Surefire config (both `**/*Test.java` and `**/*IT.java`)
4. Local `mvn test` passes on Windows (profile auto-activates)
</verification>

<success_criteria>
- All 4 pom.xml files modified with the OS-conditional profile pattern
- `grep -c "DOCKER_HOST" */pom.xml` shows matches only inside `<profiles>` blocks
- Local test suite passes on Windows with zero test regressions
- The pom.xml structure is ready for `mvn -B test` on ubuntu-latest (CI) without DOCKER_HOST override
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-ci-cd-foundation/02.1-01-SUMMARY.md`
</output>
