---
phase: 01.1-cdc-pipeline-compliance-kind-spike
verified: 2026-04-11T00:00:00Z
status: human_needed
score: 3/4 must-haves verified (SC-1/SC-2 blocked by compilation bugs; SC-3 verified; SC-4 verified)
overrides_applied: 0
gaps:
  - truth: "A transfer commit in account-service produces a CDC event on banking.transfer.events Kafka topic within 5 seconds, AND ledger-service receives and processes it (double-entry pair written)"
    status: partial
    reason: "LedgerEventListener.java has two compilation bugs (CR-01): wrong annotation element backoff (must be backOff) and wrong import org.springframework.retry.annotation.Backoff (must be org.springframework.kafka.annotation.BackOff). spring-retry is absent from ledger-service pom.xml so the import will not resolve. This blocks compilation of ledger-service, meaning LedgerEventListenerIT cannot run to confirm double-entry writes."
    artifacts:
      - path: "ledger-service/src/main/java/au/com/bankforge/ledger/kafka/LedgerEventListener.java"
        issue: "Line 13: import org.springframework.retry.annotation.Backoff — spring-retry is not on classpath; Line 32: backoff = @Backoff(...) — @RetryableTopic has no backoff element in Spring Kafka 4.x; correct form is backOff = @BackOff(...)"
      - path: "ledger-service/src/test/java/au/com/bankforge/ledger/kafka/LedgerEventListenerIT.java"
        issue: "Line 10: import org.testcontainers.containers.KafkaContainer — legacy class defaults to ZooKeeper mode; incompatible with apache/kafka:3.9.2 (KRaft-only). Container will hang or error. Must switch to org.testcontainers.kafka.KafkaContainer (new API, no asCompatibleSubstituteFor needed)"
    missing:
      - "Fix LedgerEventListener.java: change import to org.springframework.kafka.annotation.BackOff; change annotation element from backoff to backOff"
      - "Fix LedgerEventListenerIT.java: change import to org.testcontainers.kafka.KafkaContainer; remove asCompatibleSubstituteFor"
      - "Run mvn test -pl ledger-service -Dtest=LedgerEventListenerIT to confirm double-entry writes"
  - truth: "A Kafka consumer receives and processes the CDC event, with failed messages routed to the DLT topic after retry exhaustion"
    status: partial
    reason: "The DLT routing structure (NotificationEventListener, AustracAuditListener) is correct and compiles. However LedgerEventListener — the primary financial consumer — has the same CR-01 backoff/BackOff compilation bug. Until that is fixed and LedgerEventListenerIT passes, DLT behaviour for the ledger consumer path cannot be confirmed by automated test."
    artifacts:
      - path: "ledger-service/src/main/java/au/com/bankforge/ledger/kafka/LedgerEventListener.java"
        issue: "Same CR-01 bug as above — blocks compilation and test execution for ledger DLT path"
    missing:
      - "Same fix as SC-1 above; once LedgerEventListener compiles, the @RetryableTopic + @DltHandler structure is already in place"
human_verification:
  - test: "Run LedgerEventListenerIT after fixing CR-01 and CR-02"
    expected: "mvn test -pl ledger-service -Dtest=LedgerEventListenerIT exits 0; test verifies DEBIT and CREDIT LedgerEntry rows written for a transfer event"
    why_human: "Cannot run Maven tests in this environment. The compilation bugs must be fixed first, then the test result confirms SC-1 (double-entry) and SC-2 (DLT path for ledger consumer)."
  - test: "Run mvn test -pl notification-service -Dtest=AustracAuditListenerTest"
    expected: "Both tests pass: shouldLogAustracEventForTransferAboveThreshold and shouldNotLogAustracEventForTransferBelowThreshold"
    why_human: "Cannot run Maven tests in this environment. The test structure is correct (logback ListAppender, org.testcontainers.kafka.KafkaContainer, spring-boot-starter-kafka) but test execution result was not observed directly in this session."
  - test: "Run mvn test -pl notification-service -Dtest=NotificationEventListenerIT"
    expected: "Test passes — confirms notification-service independently receives the same events as ledger-service (separate consumer group)"
    why_human: "Cannot run Maven tests in this environment. Code inspection confirms correct structure."
---

# Phase 01.1: CDC Pipeline + Compliance + Kind Spike — Verification Report

**Phase Goal:** Events from the Phase 1 outbox table flow reliably from PostgreSQL WAL through Debezium and Kafka to downstream consumers, AUSTRAC audit logging captures threshold transfers, and Podman + kind networking is validated as the substrate for Phase 3.
**Verified:** 2026-04-11
**Status:** human_needed
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| SC-1 | A transfer produces a Debezium CDC event on banking.transfer.events within 5 seconds, AND ledger-service writes a double-entry LedgerEntry pair | PARTIAL | Pipeline wiring is correct (compose.yml, outbox-connector.json, aggregatetype=transfer, LedgerEventListener @KafkaListener groupId=ledger-service, @Transactional double-entry save). Blocked from test confirmation by CR-01 + CR-02 compilation bugs in ledger-service |
| SC-2 | A Kafka consumer receives and processes CDC events; failed messages route to DLT after retry exhaustion | PARTIAL | NotificationEventListener and AustracAuditListener: @RetryableTopic + @DltHandler correct (backOff=@BackOff, BackOff import). LedgerEventListener: @DltHandler present but @RetryableTopic has wrong element (backoff not backOff) and unresolvable import — compilation failure blocks ledger DLT path test |
| SC-3 | A transfer >= AUD $10,000 produces a structured compliance event with timestamp, both account IDs, amount, and transfer ID in the AUSTRAC audit log | VERIFIED | AustracAuditListener.java: BigDecimal.compareTo() threshold check against new BigDecimal("10000.00"); AUSTRAC_LOG.info with kv(transfer_id), kv(from_account_id), kv(to_account_id), kv(amount), kv(currency), kv(event_timestamp), kv(austrac_event,true); groupId=austrac-audit. logback-spring.xml: AUSTRAC_AUDIT logger additivity=false, LogstashEncoder, log.austrac.path springProperty. Test uses logback ListAppender — SUMMARY confirms both tests pass |
| SC-4 | A single-node kind cluster starts via KIND_EXPERIMENTAL_PROVIDER=podman, a test pod is reachable, and DNS resolves cluster-internal names from a pod | VERIFIED | scripts/kind-smoke-test.sh exists with KIND_EXPERIMENTAL_PROVIDER=podman, WSL2 workarounds (--disable-dns network, k8s-file log driver), kubectl wait on node+pod+CoreDNS, nslookup kubernetes.default.svc.cluster.local. STATE.md Key Decisions: kind v0.27.0, kubectl v1.35.3, Podman v5.8.1 — PASSED. Todos: [x] Run Podman + kind networking spike |

**Score:** SC-3 and SC-4 fully verified. SC-1 and SC-2 are structurally correct but blocked by two known compilation bugs in ledger-service. Status is human_needed pending test execution after bug fixes.

---

## Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `compose.yml` | Kafka KRaft + Debezium containers with health checks | VERIFIED | apache/kafka:3.9.2 with KAFKA_PROCESS_ROLES=broker,controller; quay.io/debezium/connect:3.1 with depends_on kafka service_healthy + account-db service_healthy |
| `infra/debezium/outbox-connector.json` | Debezium PostgreSQL connector with EventRouter SMT | VERIFIED | plugin.name=pgoutput; transforms.outbox.type=io.debezium.transforms.outbox.EventRouter; route.topic.replacement=banking.${routedByValue}.events; slot.drop.on.stop=true |
| `scripts/register-connector.sh` | Idempotent connector registration | VERIFIED | PUT if exists, POST if not; waits for Connect readiness |
| `ledger-service/.../LedgerEventListener.java` | Kafka consumer writing double-entry ledger pairs | STUB (compilation blocked) | @KafkaListener topics=banking.transfer.events groupId=ledger-service; @Transactional double-entry save present. CR-01: wrong annotation element (backoff vs backOff) + wrong import (spring-retry Backoff vs spring-kafka BackOff) |
| `ledger-service/src/test/.../LedgerEventListenerIT.java` | Integration test for double-entry writes | STUB (test blocked) | CR-02: imports org.testcontainers.containers.KafkaContainer (ZooKeeper-mode default) instead of org.testcontainers.kafka.KafkaContainer (KRaft-native) |
| `notification-service/.../NotificationEventListener.java` | Kafka consumer logging transfer notifications | VERIFIED | @KafkaListener groupId=notification-service; @RetryableTopic backOff=@BackOff (correct); @DltHandler |
| `notification-service/.../AustracAuditListener.java` | AUSTRAC threshold detection consumer | VERIFIED | @KafkaListener groupId=austrac-audit; BigDecimal.compareTo(THRESHOLD); AUSTRAC_LOG via LoggerFactory.getLogger("AUSTRAC_AUDIT"); all required kv fields; @RetryableTopic backOff=@BackOff (correct); @DltHandler |
| `notification-service/src/main/resources/logback-spring.xml` | AUSTRAC_AUDIT logger with dedicated file appender | VERIFIED | AUSTRAC_AUDIT logger additivity=false; AUSTRAC_FILE RollingFileAppender with LogstashEncoder; log.austrac.path springProperty |
| `notification-service/src/test/.../AustracAuditListenerTest.java` | Integration test for threshold detection | VERIFIED | org.testcontainers.kafka.KafkaContainer (correct, KRaft-native); logback ListAppender for in-memory assertions; @Order(1) above-threshold, @Order(2) below-threshold |
| `scripts/kind-smoke-test.sh` | kind + Podman networking spike script | VERIFIED | KIND_EXPERIMENTAL_PROVIDER=podman; WSL2 workarounds baked in; cleanup trap; FQDN DNS test; SPIKE PASSED output |

---

## Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| account-service outbox_event table | Debezium Connect | PostgreSQL WAL wal_level=logical + pgoutput plugin | VERIFIED | compose.yml: account-db postgres cmd has wal_level=logical, max_replication_slots=5; connector uses plugin.name=pgoutput, slot.name=bankforge_outbox_slot |
| Debezium Connect | banking.transfer.events Kafka topic | EventRouter SMT: aggregatetype=transfer → banking.transfer.events | VERIFIED | outbox-connector.json: route.by.field=aggregatetype; route.topic.replacement=banking.${routedByValue}.events; TransferService uses aggregatetype("transfer") lowercase |
| banking.transfer.events | LedgerEventListener | @KafkaListener groupId=ledger-service | WIRED (code) | Annotation present and correct; blocked from runtime confirmation by CR-01 compilation bug |
| banking.transfer.events | NotificationEventListener | @KafkaListener groupId=notification-service | VERIFIED | Correct backOff annotation; SUMMARY confirms NotificationEventListenerIT passes |
| banking.transfer.events | AustracAuditListener | @KafkaListener groupId=austrac-audit | VERIFIED | Correct backOff annotation; SUMMARY confirms AustracAuditListenerTest passes |
| AustracAuditListener | AUSTRAC_AUDIT log channel | LoggerFactory.getLogger("AUSTRAC_AUDIT") + logback-spring.xml AUSTRAC_FILE appender | VERIFIED | additivity=false confirmed; LogstashEncoder confirmed; log.austrac.path springProperty confirmed |
| Podman machine (rootful) | kind cluster | KIND_EXPERIMENTAL_PROVIDER=podman + WSL2 workarounds | VERIFIED | STATE.md documents PASSED; tool versions: kind v0.27.0, kubectl v1.35.3, Podman v5.8.1 |
| kind cluster | test pod DNS | CoreDNS resolving kubernetes.default.svc.cluster.local | VERIFIED | STATE.md: kubernetes.default.svc.cluster.local → 10.96.0.1 confirmed |

---

## Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| LedgerEventListener | debit/credit LedgerEntry | JSON payload parsed from Kafka topic, saved via ledgerEntryRepository.save() | Yes (JPA repository write) | WIRED — data flows from Kafka topic through JSON parse to DB write; blocked from test confirmation by CR-01/CR-02 |
| AustracAuditListener | AUSTRAC_LOG ILoggingEvent | BigDecimal amount from JSON payload; compareTo(THRESHOLD) gate | Yes (conditional structured log) | FLOWING — threshold check and structured kv fields verified in code; test assertions use ListAppender |
| NotificationEventListener | log.info message | JSON payload parsed from Kafka topic | Yes (structured log output) | FLOWING — log.info with extracted fields confirmed |

---

## Behavioral Spot-Checks

Step 7b: SKIPPED — Maven tests cannot be run in this verification environment. Tests exist and are structurally correct for notification-service. ledger-service tests are blocked by compilation bugs (CR-01/CR-02). Test execution delegated to human verification.

---

## Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| CORE-04 | 01.1-01 | System records every transfer as a double-entry ledger pair via ledger-service | PARTIAL | LedgerEventListener code structure correct (@Transactional, DEBIT+CREDIT save). Blocked by CR-01 compilation bug — cannot confirm test passes |
| CORE-05 | 01.1-01 | System delivers async notifications via notification-service consuming Kafka events | VERIFIED | NotificationEventListener with correct @RetryableTopic. SUMMARY: NotificationEventListenerIT passes |
| TXNS-02 | 01.1-01, 01.1-02 | Outbox rows captured by Debezium CDC and published to Kafka without dual-write | VERIFIED | Debezium connector with EventRouter SMT wired to account-db WAL; compose.yml pipeline complete |
| TXNS-03 | 01.1-01 | Downstream services consume via Saga choreography — no central orchestrator | VERIFIED | Three separate consumer groups (ledger-service, notification-service, austrac-audit) each subscribe independently; no orchestrator |
| AUBN-01 | 01.1-01 | BSB format validation on account creation and transfer | VERIFIED (no regression) | BsbValidator.java + @ValidBsb on CreateAccountRequest confirmed present; PLAN-01 requires BsbValidatorTest passes — no regression introduced |
| AUBN-02 | 01.1-02 | Audit log flag for transfers >= AUD $10,000 with timestamp, account IDs, amount, transfer ID | VERIFIED | AustracAuditListener with BigDecimal.compareTo(), all required kv fields, dedicated log channel; AustracAuditListenerTest passes per SUMMARY |

---

## Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `ledger-service/.../LedgerEventListener.java` | 13, 32 | Wrong import (spring-retry Backoff) + wrong annotation element (backoff not backOff) — compilation failure | BLOCKER | Prevents ledger-service from compiling; LedgerEventListenerIT cannot run; SC-1 and SC-2 (ledger path) unconfirmed |
| `ledger-service/src/test/.../LedgerEventListenerIT.java` | 10, 33–36 | org.testcontainers.containers.KafkaContainer defaults to ZooKeeper mode; apache/kafka:3.9.2 is KRaft-only | BLOCKER | Test will hang or error on container startup; double-entry write confirmation blocked |
| `ledger-service/pom.xml` | ~77 | spring-kafka (not spring-boot-starter-kafka) — may lack KafkaTemplate auto-config in Spring Boot 4 | WARNING | notification-service had the same issue and required switching to spring-boot-starter-kafka (SUMMARY deviation #5). Ledger service may fail at runtime with NoSuchBeanDefinitionException for KafkaTemplate |
| `scripts/register-connector.sh` | 17, 22 | Relative path `infra/debezium/outbox-connector.json` — breaks if not run from repo root | WARNING | WR-03 from REVIEW.md; script fails with misleading error when run from scripts/ directory |
| `scripts/kind-smoke-test.sh` | 64–65 | Unconditional overwrite of /root/.config/containers/containers.conf each run | WARNING | WR-05 from REVIEW.md; destroys pre-existing Podman config on each invocation |
| `compose.yml` | multiple | Plaintext passwords (secret) committed to source control | INFO | IN-01 from REVIEW.md; low risk for local-only dev |

---

## Human Verification Required

### 1. Fix CR-01 + CR-02 then run LedgerEventListenerIT

**Test:** Apply these two fixes in ledger-service:
1. In `LedgerEventListener.java`: change `import org.springframework.retry.annotation.Backoff` to `import org.springframework.kafka.annotation.BackOff`; change `backoff = @Backoff(...)` to `backOff = @BackOff(...)`
2. In `LedgerEventListenerIT.java`: change `import org.testcontainers.containers.KafkaContainer` to `import org.testcontainers.kafka.KafkaContainer`; remove `.asCompatibleSubstituteFor("confluentinc/cp-kafka")`

Then run: `mvn test -pl ledger-service -Dtest=LedgerEventListenerIT`

**Expected:** Test exits 0. LedgerEntry repository contains 2 rows for the test transfer — one DEBIT and one CREDIT.

**Why human:** Cannot compile or run Maven tests in this verification environment. The code structure is correct and the same pattern as notification-service (which passes), but compilation must succeed before any test result is meaningful.

### 2. Verify ledger-service spring-kafka auto-config after fix

**Test:** After applying the CR-01 fix, check whether `mvn compile -pl ledger-service` succeeds. If it does, confirm `mvn test -pl ledger-service` also passes (not just the one IT). If NoSuchBeanDefinitionException for KafkaTemplate occurs, switch pom.xml from `spring-kafka` to `spring-boot-starter-kafka` (same fix as notification-service).

**Expected:** ledger-service compiles and KafkaTemplate is auto-configured.

**Why human:** The ledger-service uses `spring-kafka` (not `spring-boot-starter-kafka`), which is the same root cause that required the notification-service pom change (SUMMARY deviation #5). This may or may not affect ledger-service depending on whether Spring Boot's main `spring-boot-autoconfigure` transitively covers spring-kafka.

### 3. Run AustracAuditListenerTest to confirm SC-3

**Test:** `mvn test -pl notification-service -Dtest=AustracAuditListenerTest`

**Expected:** Both `shouldLogAustracEventForTransferAboveThreshold` and `shouldNotLogAustracEventForTransferBelowThreshold` pass. SUMMARY confirms this but direct observation is authoritative.

**Why human:** Cannot run Maven tests in this environment.

---

## Gaps Summary

The phase goal is structurally achieved — all pipeline components exist, are wired, and follow the correct patterns. Two known compilation bugs in `ledger-service` (CR-01: wrong annotation element + import; CR-02: legacy KafkaContainer class) prevent test execution for success criteria 1 and 2. These are well-understood, isolated fixes with clear remediation steps (documented in 01.1-REVIEW.md). The AUSTRAC audit (SC-3) and kind spike (SC-4) are fully verified.

SC-1 and SC-2 require the two fixes to be applied and `LedgerEventListenerIT` to pass before this phase can be marked complete. All other artifacts — AustracAuditListener, NotificationEventListener, Debezium connector, compose.yml, logback-spring.xml, kind-smoke-test.sh, STATE.md — are verified correct.

---

_Verified: 2026-04-11_
_Verifier: Claude (gsd-verifier)_
