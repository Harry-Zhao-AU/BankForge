---
status: partial
phase: 01.1-cdc-pipeline-compliance-kind-spike
source: [01.1-VERIFICATION.md]
started: 2026-04-11T00:00:00Z
updated: 2026-04-11T00:00:00Z
---

## Current Test

[awaiting compilation fixes + test execution]

## Tests

### 1. Fix CR-01 + CR-02 then run LedgerEventListenerIT

**Step 1:** In `ledger-service/src/main/java/au/com/bankforge/ledger/kafka/LedgerEventListener.java`:
- Change `import org.springframework.retry.annotation.Backoff` → `import org.springframework.kafka.annotation.BackOff`
- Change `backoff = @Backoff(...)` → `backOff = @BackOff(...)`

**Step 2:** In `ledger-service/src/test/java/au/com/bankforge/ledger/kafka/LedgerEventListenerIT.java`:
- Change `import org.testcontainers.containers.KafkaContainer` → `import org.testcontainers.kafka.KafkaContainer`
- Remove `.asCompatibleSubstituteFor("confluentinc/cp-kafka")`

**Step 3:** `mvn test -pl ledger-service -Dtest=LedgerEventListenerIT`

expected: Test exits 0. LedgerEntry repository contains DEBIT + CREDIT rows for the test transfer.
result: [pending]

### 2. Verify ledger-service spring-kafka auto-config after CR-01 fix

**Test:** `mvn compile -pl ledger-service` succeeds; `mvn test -pl ledger-service` passes.
If `NoSuchBeanDefinitionException` for KafkaTemplate: switch pom.xml from `spring-kafka` → `spring-boot-starter-kafka` (same fix as notification-service).

expected: ledger-service compiles and KafkaTemplate is auto-configured.
result: [pending]

### 3. Run AustracAuditListenerTest to confirm SC-3

**Test:** `mvn test -pl notification-service -Dtest=AustracAuditListenerTest`

expected: Both `shouldLogAustracEventForTransferAboveThreshold` and `shouldNotLogAustracEventForTransferBelowThreshold` pass.
result: [pending]

## Summary

total: 3
passed: 0
issues: 0
pending: 3
skipped: 0
blocked: 0

## Gaps
