---
phase: 03-service-mesh-auth
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: false
requirements: [MESH-01]
files_modified:
  - infra/kind/kind-config.yaml
  - scripts/k8s-cluster-up.sh
  - k8s/namespace.yaml
  - k8s/infrastructure/account-db.yaml
  - k8s/infrastructure/payment-db.yaml
  - k8s/infrastructure/ledger-db.yaml
  - k8s/infrastructure/notification-db.yaml
  - k8s/infrastructure/redis.yaml
  - k8s/infrastructure/kafka.yaml
  - k8s/infrastructure/debezium.yaml

must_haves:
  truths:
    - "kind cluster named 'bankforge' is up with node kindest/node:v1.32.2 and Istio 1.29.2 control plane running in istio-system"
    - "bankforge namespace exists with label istio-injection=enabled"
    - "All infrastructure pods (4x PostgreSQL, Redis, Kafka, Debezium) show 2/2 READY with Istio sidecar in PERMISSIVE mode"
    - "PostgreSQL pods pass readinessProbe pg_isready with wal_level=logical active"
    - "Kafka runs KRaft mode — no ZooKeeper pod exists"
    - "Debezium Kafka Connect REST API responds on port 8083 and outbox connectors are registered"
  artifacts:
    - path: "infra/kind/kind-config.yaml"
      provides: "Single-node kind cluster config with pinned node image and Kong port mappings"
      contains: "kindest/node:v1.32.2"
    - path: "scripts/k8s-cluster-up.sh"
      provides: "Bootstrap script: kind cluster creation + Istio install + namespace labelling"
      contains: "KIND_EXPERIMENTAL_PROVIDER=podman"
    - path: "k8s/namespace.yaml"
      provides: "bankforge namespace with istio-injection label"
      contains: "istio-injection: enabled"
    - path: "k8s/infrastructure/account-db.yaml"
      provides: "PostgreSQL 17 StatefulSet + Service + Secret for account-service"
      contains: "wal_level=logical"
    - path: "k8s/infrastructure/kafka.yaml"
      provides: "Kafka 3.9.2 KRaft StatefulSet + Service"
      contains: "KAFKA_PROCESS_ROLES: broker,controller"
    - path: "k8s/infrastructure/debezium.yaml"
      provides: "Debezium Kafka Connect Deployment with ConfigMaps for entrypoint + connector JSONs"
      contains: "debezium-entrypoint.sh"
  key_links:
    - from: "debezium.yaml (Deployment command)"
      to: "debezium-entrypoint ConfigMap"
      via: "volumeMount at /connectors + command override"
      pattern: "debezium-entrypoint.sh"
    - from: "account-outbox-connector.json (database.hostname)"
      to: "account-db Service"
      via: "K8s DNS — same namespace resolution"
      pattern: "account-db"
    - from: "kafka.yaml (KAFKA_ADVERTISED_LISTENERS)"
      to: "kafka Service"
      via: "PLAINTEXT://kafka:9092"
      pattern: "kafka:9092"
---

<objective>
Bootstrap a single-node kind Kubernetes cluster with Istio 1.29.2 in PERMISSIVE mTLS mode and deploy all infrastructure components: 4 PostgreSQL StatefulSets (one per service), Redis, Kafka KRaft, and Debezium Kafka Connect with outbox connector auto-registration.

Purpose: This is the foundation everything in Phase 3 runs on. No banking services, no auth, no service mesh enforcement yet — just the cluster up and all infra pods healthy with Istio sidecars injected.

Output: `infra/kind/kind-config.yaml`, `scripts/k8s-cluster-up.sh`, `k8s/namespace.yaml`, 7 infrastructure manifests in `k8s/infrastructure/`.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-service-mesh-auth/03-CONTEXT.md
@.planning/phases/03-service-mesh-auth/03-RESEARCH.md
@compose.yml
@infra/debezium/debezium-entrypoint.sh
@infra/debezium/account-outbox-connector.json
@infra/debezium/ledger-outbox-connector.json

<interfaces>
<!-- WSL2 Podman rootful workarounds validated in Phase 1.1 spike (STATE.md Key Decisions): -->
<!-- 1. KIND_EXPERIMENTAL_PROVIDER=podman env var required -->
<!-- 2. `podman network create --disable-dns kind` must be run before kind create (prevents aardvark-dns failure) -->
<!-- 3. Podman log_driver=k8s-file required — one-time setup in /etc/containers/containers.conf -->
<!-- 4. These workarounds were VALIDATED in the kind spike; do not change them -->

<!-- Compose infra images (exact versions to port): -->
<!-- postgres:17-alpine, redis:7.2-alpine, apache/kafka:3.9.2, quay.io/debezium/connect:3.1 -->

<!-- Kafka CLUSTER_ID from compose.yml must be REUSED: MkU3OEVBNTcwNTJENDM2Qk -->
<!-- (Kafka KRaft uses this to identify the cluster; changing it causes broker rejection) -->

<!-- DB credentials from compose.yml: -->
<!-- account: password=secret, db=accountdb -->
<!-- payment: password=secret, db=paymentdb -->
<!-- ledger:  password=secret, db=ledgerdb -->
<!-- notification: password=secret, db=notificationdb -->

<!-- Service port in K8s: containerPort 8080 (Spring Boot default) -->
<!-- Kong port mappings on kind node: 30080 (HTTP), 30443 (HTTPS) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create kind cluster config, bootstrap script, and namespace manifest</name>
  <files>
    infra/kind/kind-config.yaml,
    scripts/k8s-cluster-up.sh,
    k8s/namespace.yaml
  </files>
  <read_first>
    - .planning/phases/03-service-mesh-auth/03-RESEARCH.md (Section 1: kind Node Image, Section 2: Istio install commands)
    - .planning/STATE.md (Key Decisions — Podman rootful WSL2 workarounds from kind spike)
  </read_first>
  <action>
Create three files.

**1. `infra/kind/kind-config.yaml`**

Create directory `infra/kind/` and the cluster config file:

```yaml
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
name: bankforge
nodes:
  - role: control-plane
    image: kindest/node:v1.32.2@sha256:f226345927d7e348497136874b6d207e0b32cc52154ad8323129352923a3142f
    extraPortMappings:
      - containerPort: 30080
        hostPort: 30080
        protocol: TCP
      - containerPort: 30443
        hostPort: 30443
        protocol: TCP
```

Notes:
- `extraPortMappings` exposes NodePort 30080/30443 to Windows host — Kong will use these in Plan 03-03.
- No `disableDefaultCNI` — kind's default CNI (kindnet) works fine with Podman rootful.
- Single control-plane node only — no workers (dev cluster).

---

**2. `scripts/k8s-cluster-up.sh`**

Create this script. It handles the full cluster bootstrap including Podman WSL2 workarounds:

```bash
#!/usr/bin/env bash
# Bootstrap the BankForge kind cluster with Istio.
# Prerequisites: kind v0.27.0, kubectl v1.35.3, Podman rootful running in WSL2.
# Run from repo root: bash scripts/k8s-cluster-up.sh
set -euo pipefail

CLUSTER_NAME="bankforge"
ISTIO_VERSION="1.29.2"
NAMESPACE="bankforge"

echo "=== BankForge kind cluster bootstrap ==="
echo ""

# ── Podman WSL2 pre-flight ─────────────────────────────────────────────────

echo "[1/6] Checking Podman log driver..."
LOG_DRIVER=$(podman info --format '{{.Host.LogDriver}}' 2>/dev/null || echo "unknown")
if [ "$LOG_DRIVER" != "k8s-file" ]; then
  echo "WARNING: Podman log_driver is '$LOG_DRIVER', expected 'k8s-file'."
  echo "kind may fail to read container logs without k8s-file."
  echo "Fix (run as root in WSL2):"
  echo "  echo '[containers]' >> /etc/containers/containers.conf"
  echo "  echo 'log_driver = \"k8s-file\"' >> /etc/containers/containers.conf"
  echo "Then restart Podman and re-run this script."
  echo "Continuing anyway — cluster creation may still succeed..."
fi

echo "[2/6] Ensuring 'kind' Podman network exists (--disable-dns to avoid aardvark-dns conflicts)..."
if podman network exists kind 2>/dev/null; then
  echo "  'kind' network already exists — skipping creation."
else
  podman network create --disable-dns kind
  echo "  Created 'kind' network with --disable-dns."
fi

# ── Cluster creation ───────────────────────────────────────────────────────

echo "[3/6] Creating kind cluster '$CLUSTER_NAME'..."
if kind get clusters 2>/dev/null | grep -q "^${CLUSTER_NAME}$"; then
  echo "  Cluster '$CLUSTER_NAME' already exists — skipping creation."
else
  KIND_EXPERIMENTAL_PROVIDER=podman kind create cluster \
    --config infra/kind/kind-config.yaml \
    --name "$CLUSTER_NAME" \
    --wait 120s
  echo "  Cluster created."
fi

echo "  Verifying nodes..."
kubectl get nodes --context "kind-${CLUSTER_NAME}"

# ── Istio installation ─────────────────────────────────────────────────────

echo "[4/6] Installing Istio ${ISTIO_VERSION}..."
if kubectl get namespace istio-system --context "kind-${CLUSTER_NAME}" &>/dev/null; then
  echo "  istio-system namespace already exists — skipping Istio install."
else
  ISTIO_DIR="istio-${ISTIO_VERSION}"
  if [ ! -d "$ISTIO_DIR" ]; then
    curl -L https://istio.io/downloadIstio | ISTIO_VERSION="${ISTIO_VERSION}" sh -
  fi
  export PATH="${PWD}/${ISTIO_DIR}/bin:${PATH}"

  istioctl install --set profile=demo --context "kind-${CLUSTER_NAME}" -y

  echo "  Waiting for Istio control plane to be ready..."
  kubectl wait --for=condition=Ready pods --all -n istio-system \
    --timeout=300s --context "kind-${CLUSTER_NAME}"

  echo "  Verifying install..."
  istioctl verify-install --context "kind-${CLUSTER_NAME}"
fi

# ── Namespace ──────────────────────────────────────────────────────────────

echo "[5/6] Creating and labelling '${NAMESPACE}' namespace..."
kubectl apply -f k8s/namespace.yaml --context "kind-${CLUSTER_NAME}"

# ── Secrets ───────────────────────────────────────────────────────────────

echo "[6/6] Creating database secrets in '${NAMESPACE}'..."
for svc in account payment ledger notification; do
  SECRET_NAME="${svc}-db-secret"
  if kubectl get secret "$SECRET_NAME" -n "$NAMESPACE" \
      --context "kind-${CLUSTER_NAME}" &>/dev/null; then
    echo "  Secret $SECRET_NAME already exists — skipping."
  else
    kubectl create secret generic "$SECRET_NAME" \
      --from-literal=password=secret \
      -n "$NAMESPACE" \
      --context "kind-${CLUSTER_NAME}"
    echo "  Created $SECRET_NAME."
  fi
done

echo ""
echo "=== Bootstrap complete ==="
echo ""
echo "Next steps:"
echo "  1. Apply infrastructure:  kubectl apply -f k8s/infrastructure/ -n bankforge"
echo "  2. Verify pods (2/2):     kubectl get pods -n bankforge -w"
echo "  3. Continue to Plan 03-02 (banking services + Istio STRICT)"
```

Make the script executable: `chmod +x scripts/k8s-cluster-up.sh`

---

**3. `k8s/namespace.yaml`**

Create `k8s/` directory and `k8s/namespace.yaml`:

```yaml
apiVersion: v1
kind: Namespace
metadata:
  name: bankforge
  labels:
    istio-injection: enabled
```

The `istio-injection: enabled` label must be on the namespace BEFORE pods are created, so sidecar injection is active from first pod startup.
  </action>
  <verify>
    <automated>test -f infra/kind/kind-config.yaml && grep -q "v1.32.2" infra/kind/kind-config.yaml && echo "kind-config OK" && test -f scripts/k8s-cluster-up.sh && grep -q "KIND_EXPERIMENTAL_PROVIDER=podman" scripts/k8s-cluster-up.sh && grep -q "disable-dns" scripts/k8s-cluster-up.sh && echo "bootstrap script OK" && test -f k8s/namespace.yaml && grep -q "istio-injection: enabled" k8s/namespace.yaml && echo "namespace OK"</automated>
  </verify>
  <acceptance_criteria>
    - `infra/kind/kind-config.yaml` exists with `kindest/node:v1.32.2` image and SHA256 digest
    - `infra/kind/kind-config.yaml` contains `extraPortMappings` for ports 30080 and 30443
    - `scripts/k8s-cluster-up.sh` is executable and contains `KIND_EXPERIMENTAL_PROVIDER=podman`
    - `scripts/k8s-cluster-up.sh` contains `podman network create --disable-dns kind`
    - `scripts/k8s-cluster-up.sh` contains `istioctl install --set profile=demo`
    - `scripts/k8s-cluster-up.sh` creates DB secrets for all 4 services
    - `k8s/namespace.yaml` has `name: bankforge` and `istio-injection: enabled` label
  </acceptance_criteria>
  <done>kind cluster config, bootstrap script, and namespace manifest created. Bootstrap script handles Podman WSL2 workarounds (log_driver check, --disable-dns kind network), installs Istio 1.29.2 demo profile, labels bankforge namespace, and creates DB secrets.</done>
</task>

<task type="auto">
  <name>Task 2: Create PostgreSQL StatefulSets (4x), Redis Deployment, and Kafka StatefulSet</name>
  <files>
    k8s/infrastructure/account-db.yaml,
    k8s/infrastructure/payment-db.yaml,
    k8s/infrastructure/ledger-db.yaml,
    k8s/infrastructure/notification-db.yaml,
    k8s/infrastructure/redis.yaml,
    k8s/infrastructure/kafka.yaml
  </files>
  <read_first>
    - compose.yml (exact image versions, env vars, and postgres -c args to port)
    - .planning/phases/03-service-mesh-auth/03-RESEARCH.md (Section 7: resource limits)
  </read_first>
  <action>
Create `k8s/infrastructure/` directory and 6 manifest files.

---

**`k8s/infrastructure/account-db.yaml`**

```yaml
apiVersion: v1
kind: Service
metadata:
  name: account-db
  namespace: bankforge
spec:
  selector:
    app: account-db
  ports:
    - port: 5432
      targetPort: 5432
  clusterIP: None  # headless — StatefulSet DNS: account-db-0.account-db.bankforge.svc.cluster.local
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: account-db
  namespace: bankforge
spec:
  serviceName: account-db
  replicas: 1
  selector:
    matchLabels:
      app: account-db
  template:
    metadata:
      labels:
        app: account-db
    spec:
      containers:
        - name: postgres
          image: postgres:17-alpine
          command:
            - postgres
            - -c
            - wal_level=logical
            - -c
            - max_replication_slots=5
            - -c
            - max_wal_senders=5
          env:
            - name: POSTGRES_DB
              value: accountdb
            - name: POSTGRES_USER
              value: account
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: account-db-secret
                  key: password
          ports:
            - containerPort: 5432
          readinessProbe:
            exec:
              command: [pg_isready, -U, account, -d, accountdb]
            initialDelaySeconds: 10
            periodSeconds: 5
            failureThreshold: 6
          resources:
            requests:
              cpu: "250m"
              memory: "256Mi"
            limits:
              cpu: "1000m"
              memory: "512Mi"
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
      volumes:
        - name: data
          emptyDir: {}
```

---

**`k8s/infrastructure/payment-db.yaml`** — identical structure, substitute:
- `name: payment-db` (Service + StatefulSet metadata)
- `app: payment-db` (selector + label)
- `serviceName: payment-db`
- `POSTGRES_DB: paymentdb`
- `POSTGRES_USER: payment`
- `secretKeyRef.name: payment-db-secret`
- readinessProbe: `[pg_isready, -U, payment, -d, paymentdb]`

---

**`k8s/infrastructure/ledger-db.yaml`** — identical structure, substitute:
- `name: ledger-db`
- `app: ledger-db`
- `serviceName: ledger-db`
- `POSTGRES_DB: ledgerdb`
- `POSTGRES_USER: ledger`
- `secretKeyRef.name: ledger-db-secret`
- readinessProbe: `[pg_isready, -U, ledger, -d, ledgerdb]`

---

**`k8s/infrastructure/notification-db.yaml`** — identical structure, substitute:
- `name: notification-db`
- `app: notification-db`
- `serviceName: notification-db`
- `POSTGRES_DB: notificationdb`
- `POSTGRES_USER: notification`
- `secretKeyRef.name: notification-db-secret`
- readinessProbe: `[pg_isready, -U, notification, -d, notificationdb]`

---

**`k8s/infrastructure/redis.yaml`**

```yaml
apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: bankforge
spec:
  selector:
    app: redis
  ports:
    - port: 6379
      targetPort: 6379
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: redis
  namespace: bankforge
spec:
  replicas: 1
  selector:
    matchLabels:
      app: redis
  template:
    metadata:
      labels:
        app: redis
    spec:
      containers:
        - name: redis
          image: redis:7.2-alpine
          ports:
            - containerPort: 6379
          readinessProbe:
            exec:
              command: [redis-cli, ping]
            initialDelaySeconds: 5
            periodSeconds: 5
            failureThreshold: 5
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "256Mi"
          volumeMounts:
            - name: data
              mountPath: /data
      volumes:
        - name: data
          emptyDir: {}
```

---

**`k8s/infrastructure/kafka.yaml`**

```yaml
apiVersion: v1
kind: Service
metadata:
  name: kafka
  namespace: bankforge
spec:
  selector:
    app: kafka
  ports:
    - name: broker
      port: 9092
      targetPort: 9092
    - name: controller
      port: 9093
      targetPort: 9093
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: kafka
  namespace: bankforge
spec:
  serviceName: kafka
  replicas: 1
  selector:
    matchLabels:
      app: kafka
  template:
    metadata:
      labels:
        app: kafka
    spec:
      containers:
        - name: kafka
          image: apache/kafka:3.9.2
          env:
            - name: KAFKA_NODE_ID
              value: "1"
            - name: KAFKA_PROCESS_ROLES
              value: broker,controller
            - name: KAFKA_LISTENERS
              value: PLAINTEXT://0.0.0.0:9092,CONTROLLER://0.0.0.0:9093
            - name: KAFKA_ADVERTISED_LISTENERS
              value: PLAINTEXT://kafka:9092
            - name: KAFKA_CONTROLLER_LISTENER_NAMES
              value: CONTROLLER
            - name: KAFKA_LISTENER_SECURITY_PROTOCOL_MAP
              value: CONTROLLER:PLAINTEXT,PLAINTEXT:PLAINTEXT
            - name: KAFKA_CONTROLLER_QUORUM_VOTERS
              value: 1@kafka:9093
            - name: KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR
              value: "1"
            - name: KAFKA_TRANSACTION_STATE_LOG_REPLICATION_FACTOR
              value: "1"
            - name: KAFKA_TRANSACTION_STATE_LOG_MIN_ISR
              value: "1"
            - name: CLUSTER_ID
              value: MkU3OEVBNTcwNTJENDM2Qk
            - name: KAFKA_AUTO_CREATE_TOPICS_ENABLE
              value: "true"
            - name: KAFKA_LOG_RETENTION_HOURS
              value: "168"
            - name: KAFKA_HEAP_OPTS
              value: "-Xmx1g -Xms512m"
          ports:
            - containerPort: 9092
            - containerPort: 9093
          readinessProbe:
            exec:
              command:
                - /bin/sh
                - -c
                - /opt/kafka/bin/kafka-topics.sh --bootstrap-server kafka:9092 --list
            initialDelaySeconds: 20
            periodSeconds: 10
            failureThreshold: 10
          resources:
            requests:
              cpu: "500m"
              memory: "1Gi"
            limits:
              cpu: "2000m"
              memory: "2Gi"
          volumeMounts:
            - name: data
              mountPath: /var/lib/kafka/data
      volumes:
        - name: data
          emptyDir: {}
```

IMPORTANT: `KAFKA_ADVERTISED_LISTENERS` drops the `PLAINTEXT_HOST://localhost:29092` entry that Compose had for host access. K8s pods communicate via `kafka:9092` Service — no host-accessible port needed in Plan 03-01. (Port-forward in port-forward.sh covers debugging access.)

`CLUSTER_ID: MkU3OEVBNTcwNTJENDM2Qk` is reused from compose.yml — Kafka KRaft will reject a broker that presents a different cluster ID for the same data directory. Since we use emptyDir (fresh every pod restart), this value technically doesn't matter — but using the same one keeps the config consistent and avoids confusion.
  </action>
  <verify>
    <automated>for f in k8s/infrastructure/account-db.yaml k8s/infrastructure/payment-db.yaml k8s/infrastructure/ledger-db.yaml k8s/infrastructure/notification-db.yaml k8s/infrastructure/redis.yaml k8s/infrastructure/kafka.yaml; do echo "=== $f ===" && test -f "$f" && echo "EXISTS" && grep -c "emptyDir" "$f" && grep -c "resources:" "$f"; done</automated>
  </verify>
  <acceptance_criteria>
    - All 6 files exist in `k8s/infrastructure/`
    - Each PostgreSQL YAML contains `wal_level=logical`, `max_replication_slots=5`, `max_wal_senders=5` in command args
    - Each PostgreSQL YAML uses `secretKeyRef` (no hardcoded passwords in env)
    - Each PostgreSQL YAML has a `readinessProbe` using `pg_isready`
    - `kafka.yaml` contains `KAFKA_PROCESS_ROLES: broker,controller` (KRaft, no ZooKeeper)
    - `kafka.yaml` does NOT contain `PLAINTEXT_HOST` listener (no host exposure)
    - `kafka.yaml` contains `CLUSTER_ID: MkU3OEVBNTcwNTJENDM2Qk`
    - All 6 files use `emptyDir: {}` for storage (no PVC)
    - All 6 files have `resources:` blocks with `requests` and `limits`
  </acceptance_criteria>
  <done>4 PostgreSQL StatefulSets (with wal_level=logical for Debezium CDC), Redis Deployment, and Kafka KRaft StatefulSet created. All use emptyDir storage, resource limits, and readinessProbes. No hardcoded passwords — all use secretKeyRef.</done>
</task>

<task type="auto">
  <name>Task 3: Create Debezium Kafka Connect manifest with auto-registration ConfigMaps</name>
  <files>
    k8s/infrastructure/debezium.yaml
  </files>
  <read_first>
    - infra/debezium/debezium-entrypoint.sh (exact script content to embed in ConfigMap)
    - infra/debezium/account-outbox-connector.json (exact JSON to embed in ConfigMap)
    - infra/debezium/ledger-outbox-connector.json (exact JSON to embed in ConfigMap)
    - compose.yml (Debezium env vars: BOOTSTRAP_SERVERS, GROUP_ID, topic names, converters)
  </read_first>
  <action>
Create `k8s/infrastructure/debezium.yaml` with three sections: two ConfigMaps and a Deployment.

The approach mirrors Compose: the `debezium-entrypoint.sh` script starts Kafka Connect in background, polls until the REST API is ready, then POSTs each `*-connector.json` to register the outbox connectors.

In K8s the script and connector JSONs are embedded in ConfigMaps and mounted at `/connectors/`.

```yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: debezium-entrypoint
  namespace: bankforge
data:
  debezium-entrypoint.sh: |
    #!/bin/sh
    # Start Debezium Connect, then register all outbox connectors once the REST API is ready.
    set -e

    /docker-entrypoint.sh start &

    echo "Waiting for Kafka Connect REST API..."
    until curl -sf http://localhost:8083/connectors > /dev/null 2>&1; do
      sleep 2
    done
    echo "Kafka Connect is ready. Registering connectors..."

    for f in /connectors/*-connector.json; do
      name=$(grep -o '"name":"[^"]*"' "$f" | head -1 | cut -d'"' -f4)
      code=$(curl -s -o /dev/null -w "%{http_code}" \
        -X POST -H "Content-Type: application/json" \
        --data "@$f" http://localhost:8083/connectors)
      case "$code" in
        201) echo "[OK]   $name created" ;;
        409) echo "[SKIP] $name already exists" ;;
        *)   echo "[FAIL] $name — HTTP $code"; exit 1 ;;
      esac
    done

    echo "All connectors registered."
    wait
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: debezium-connectors
  namespace: bankforge
data:
  account-outbox-connector.json: |
    {
      "name": "account-outbox-connector",
      "config": {
        "connector.class": "io.debezium.connector.postgresql.PostgresConnector",
        "tasks.max": "1",
        "database.hostname": "account-db",
        "database.port": "5432",
        "database.user": "account",
        "database.password": "secret",
        "database.dbname": "accountdb",
        "topic.prefix": "bankforge",
        "plugin.name": "pgoutput",
        "publication.autocreate.mode": "filtered",
        "publication.name": "bankforge_outbox_pub",
        "slot.name": "bankforge_outbox_slot",
        "slot.drop.on.stop": "true",
        "table.include.list": "public.outbox_event",
        "tombstones.on.delete": "false",
        "transforms": "outbox",
        "transforms.outbox.type": "io.debezium.transforms.outbox.EventRouter",
        "transforms.outbox.route.by.field": "aggregatetype",
        "transforms.outbox.table.field.event.key": "aggregateid",
        "transforms.outbox.table.field.event.payload": "payload",
        "transforms.outbox.expand.json.payload": "true",
        "transforms.outbox.route.topic.replacement": "banking.${routedByValue}.events",
        "transforms.outbox.table.fields.additional.placement": "aggregateid:header:transaction-id,traceparent:header:traceparent",
        "key.converter": "org.apache.kafka.connect.json.JsonConverter",
        "key.converter.schemas.enable": "false",
        "value.converter": "org.apache.kafka.connect.json.JsonConverter",
        "value.converter.schemas.enable": "false"
      }
    }
  ledger-outbox-connector.json: |
    {
      "name": "ledger-outbox-connector",
      "config": {
        "connector.class": "io.debezium.connector.postgresql.PostgresConnector",
        "tasks.max": "1",
        "database.hostname": "ledger-db",
        "database.port": "5432",
        "database.user": "ledger",
        "database.password": "secret",
        "database.dbname": "ledgerdb",
        "topic.prefix": "bankforge",
        "plugin.name": "pgoutput",
        "publication.autocreate.mode": "filtered",
        "publication.name": "bankforge_ledger_outbox_pub",
        "slot.name": "bankforge_ledger_outbox_slot",
        "slot.drop.on.stop": "true",
        "table.include.list": "public.outbox_event",
        "tombstones.on.delete": "false",
        "transforms": "outbox",
        "transforms.outbox.type": "io.debezium.transforms.outbox.EventRouter",
        "transforms.outbox.route.by.field": "aggregatetype",
        "transforms.outbox.table.field.event.key": "aggregateid",
        "transforms.outbox.table.field.event.payload": "payload",
        "transforms.outbox.expand.json.payload": "true",
        "transforms.outbox.route.topic.replacement": "banking.${routedByValue}.events",
        "transforms.outbox.table.fields.additional.placement": "aggregateid:header:transaction-id,traceparent:header:traceparent",
        "key.converter": "org.apache.kafka.connect.json.JsonConverter",
        "key.converter.schemas.enable": "false",
        "value.converter": "org.apache.kafka.connect.json.JsonConverter",
        "value.converter.schemas.enable": "false"
      }
    }
---
apiVersion: v1
kind: Service
metadata:
  name: debezium
  namespace: bankforge
spec:
  selector:
    app: debezium
  ports:
    - name: rest
      port: 8083
      targetPort: 8083
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: debezium
  namespace: bankforge
spec:
  replicas: 1
  selector:
    matchLabels:
      app: debezium
  template:
    metadata:
      labels:
        app: debezium
    spec:
      containers:
        - name: debezium
          image: quay.io/debezium/connect:3.1
          command: [/bin/sh, /connectors/debezium-entrypoint.sh]
          env:
            - name: BOOTSTRAP_SERVERS
              value: kafka:9092
            - name: GROUP_ID
              value: "1"
            - name: CONFIG_STORAGE_TOPIC
              value: debezium.configs
            - name: OFFSET_STORAGE_TOPIC
              value: debezium.offsets
            - name: STATUS_STORAGE_TOPIC
              value: debezium.status
            - name: KEY_CONVERTER
              value: org.apache.kafka.connect.json.JsonConverter
            - name: VALUE_CONVERTER
              value: org.apache.kafka.connect.json.JsonConverter
            - name: KEY_CONVERTER_SCHEMAS_ENABLE
              value: "false"
            - name: VALUE_CONVERTER_SCHEMAS_ENABLE
              value: "false"
          ports:
            - containerPort: 8083
          readinessProbe:
            httpGet:
              path: /connectors
              port: 8083
            initialDelaySeconds: 30
            periodSeconds: 10
            failureThreshold: 15
          resources:
            requests:
              cpu: "250m"
              memory: "512Mi"
            limits:
              cpu: "1000m"
              memory: "1Gi"
          volumeMounts:
            - name: entrypoint
              mountPath: /connectors/debezium-entrypoint.sh
              subPath: debezium-entrypoint.sh
            - name: connectors
              mountPath: /connectors/account-outbox-connector.json
              subPath: account-outbox-connector.json
            - name: connectors
              mountPath: /connectors/ledger-outbox-connector.json
              subPath: ledger-outbox-connector.json
      volumes:
        - name: entrypoint
          configMap:
            name: debezium-entrypoint
            defaultMode: 0755
        - name: connectors
          configMap:
            name: debezium-connectors
```

IMPORTANT details:
- `command: [/bin/sh, /connectors/debezium-entrypoint.sh]` overrides the container's default entrypoint to use our registration script, exactly as Compose did with `entrypoint`.
- `defaultMode: 0755` on the entrypoint ConfigMap makes the script executable inside the container.
- Each JSON file is mounted individually with `subPath` so they appear as files in `/connectors/`, matching what the entrypoint glob `for f in /connectors/*-connector.json` expects.
- `database.hostname: account-db` and `ledger-db` resolve via K8s DNS within the `bankforge` namespace — no FQDN needed.
- The ledger-outbox-connector.json must be read from `infra/debezium/ledger-outbox-connector.json` and embedded verbatim — do NOT invent its content.
  </action>
  <verify>
    <automated>test -f k8s/infrastructure/debezium.yaml && grep -c "debezium-entrypoint" k8s/infrastructure/debezium.yaml && grep -c "account-outbox-connector" k8s/infrastructure/debezium.yaml && grep -c "ledger-outbox-connector" k8s/infrastructure/debezium.yaml && grep -c "subPath" k8s/infrastructure/debezium.yaml && echo "debezium.yaml OK"</automated>
  </verify>
  <acceptance_criteria>
    - `k8s/infrastructure/debezium.yaml` exists
    - File contains three ConfigMaps: `debezium-entrypoint` (with `debezium-entrypoint.sh` key) and `debezium-connectors` (with both connector JSON keys)
    - Deployment `command` overrides entrypoint to `/bin/sh /connectors/debezium-entrypoint.sh`
    - Both connector JSONs are mounted with `subPath` into `/connectors/`
    - `defaultMode: 0755` is set on the entrypoint ConfigMap volume so the script is executable
    - `readinessProbe` uses `httpGet /connectors port 8083` (matches Debezium REST API health check pattern from Compose)
    - `database.hostname` values are `account-db` and `ledger-db` (not localhost or FQDNs)
    - `BOOTSTRAP_SERVERS: kafka:9092` (K8s Service name)
  </acceptance_criteria>
  <done>Debezium manifest created with two ConfigMaps (entrypoint script + connector JSONs) and a Deployment that auto-registers outbox connectors on startup. Mount pattern mirrors the Compose volume approach. Connector content copied verbatim from infra/debezium/.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Bootstrap cluster and verify all infrastructure pods Running (2/2 READY)</name>
  <files>scripts/k8s-cluster-up.sh</files>
  <action>
Run the bootstrap script and apply infrastructure manifests. Verify all pods reach 2/2 READY with Istio sidecars injected in PERMISSIVE mode.

**What was built:**
- `infra/kind/kind-config.yaml` — kind cluster config (v1.32.2, Kong port mappings)
- `scripts/k8s-cluster-up.sh` — full bootstrap: Podman pre-flight, kind create, Istio 1.29.2 demo install, namespace label, DB secrets
- `k8s/namespace.yaml` — bankforge namespace with istio-injection enabled
- 4× PostgreSQL StatefulSets, Redis Deployment, Kafka KRaft StatefulSet, Debezium Deployment

**Step-by-step verification:**

```bash
# 1. Ensure WSL2 has ≥12 GiB allocated in %USERPROFILE%\.wslconfig:
#    [wsl2]
#    memory=12GB

# 2. Ensure Podman log_driver=k8s-file is set (run as root in WSL2):
#    cat /etc/containers/containers.conf | grep log_driver
#    Expected: log_driver = "k8s-file"
#    If missing: echo -e '[containers]\nlog_driver = "k8s-file"' >> /etc/containers/containers.conf

# 3. Run bootstrap (from repo root in WSL2 shell):
bash scripts/k8s-cluster-up.sh

# 4. Verify Istio control plane is healthy:
kubectl get pods -n istio-system
# Expected: istiod, istio-ingressgateway all Running

# 5. Verify namespace label:
kubectl get namespace bankforge --show-labels
# Expected: istio-injection=enabled in LABELS column

# 6. Apply all infrastructure manifests:
kubectl apply -f k8s/infrastructure/ -n bankforge

# 7. Wait for pods to be ready (takes 2-4 min for Kafka + Debezium):
kubectl get pods -n bankforge -w

# 8. Verify all pods show 2/2 READY (app container + Istio sidecar):
kubectl get pods -n bankforge
# Expected output example:
# NAME                        READY   STATUS    RESTARTS   AGE
# account-db-0                2/2     Running   0          3m
# payment-db-0                2/2     Running   0          3m
# ledger-db-0                 2/2     Running   0          3m
# notification-db-0           2/2     Running   0          3m
# redis-xxxxxxxxxx-xxxxx      2/2     Running   0          3m
# kafka-0                     2/2     Running   0          3m
# debezium-xxxxxxxxxx-xxxxx   2/2     Running   0          4m

# 9. Verify Debezium connector registration:
kubectl exec -n bankforge deploy/debezium -- \
  curl -sf http://localhost:8083/connectors | tr ',' '\n'
# Expected: ["account-outbox-connector","ledger-outbox-connector"]

# 10. Verify Kafka is in KRaft mode (no ZooKeeper pods):
kubectl get pods -n bankforge | grep -i zookeeper
# Expected: (no output)

# 11. Verify PostgreSQL wal_level=logical on account-db:
kubectl exec -n bankforge account-db-0 -- \
  psql -U account -d accountdb -c "SHOW wal_level;"
# Expected: wal_level = logical
```

**If any pod is stuck in Init or 0/1:**
- `kubectl describe pod <name> -n bankforge` — check Events section
- `kubectl logs <name> -n bankforge -c istio-proxy` — check sidecar logs
- For Debezium: `kubectl logs deploy/debezium -n bankforge` — check connector registration output

**If Debezium shows "[FAIL]" for connector registration:**
- Likely cause: Kafka not ready when Debezium started. Restart: `kubectl rollout restart deploy/debezium -n bankforge`

Type "verified" when all pods show 2/2 READY and connector list returns both connector names.
  </action>
  <verify>
    <automated>test -f k8s/infrastructure/account-db.yaml && test -f k8s/infrastructure/payment-db.yaml && test -f k8s/infrastructure/ledger-db.yaml && test -f k8s/infrastructure/notification-db.yaml && test -f k8s/infrastructure/redis.yaml && test -f k8s/infrastructure/kafka.yaml && test -f k8s/infrastructure/debezium.yaml && echo "All 7 infrastructure manifests exist"</automated>
  </verify>
  <done>Cluster bootstrap complete: kind cluster running, Istio 1.29.2 installed in PERMISSIVE mode, all infrastructure pods 2/2 READY with Istio sidecars. Debezium connectors registered. Ready for Plan 03-02 (banking services + Istio STRICT).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Host → kind cluster | kind maps ports 30080/30443 to Windows host; no auth at this layer in Plan 03-01 |
| Pod → PostgreSQL | Password in K8s Secret (created by bootstrap script); not in YAML |
| ConfigMap → Debezium | Connector JSONs contain DB password as plaintext — acceptable for dev cluster; Phase 6 would use external secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|---|---|---|---|---|
| T-03-01-01 | Information Disclosure | debezium-connectors ConfigMap (DB password in JSON) | accept | Plaintext password in ConfigMap is acceptable for local dev cluster. Phase 6 (Azure) would use K8s ExternalSecrets or Azure Key Vault references. |
| T-03-01-02 | Elevation of Privilege | Istio PERMISSIVE mode | accept | PERMISSIVE is intentional in Plan 03-01 — allows unencrypted traffic while we verify all pods have sidecars. Switched to STRICT in Plan 03-02. |
| T-03-01-03 | Denial of Service | Kafka emptyDir | accept | emptyDir means Kafka data is lost on pod restart. Acceptable for dev — cluster is re-seeded. Production would use PersistentVolumes. |
</threat_model>

<verification>
1. All 10 files exist (kind-config.yaml, bootstrap script, namespace.yaml, 7 infra manifests)
2. Bootstrap script contains all Podman WSL2 workarounds (log_driver check, --disable-dns, KIND_EXPERIMENTAL_PROVIDER)
3. All PostgreSQL StatefulSets use wal_level=logical (required for Debezium CDC)
4. Kafka StatefulSet uses KRaft mode (KAFKA_PROCESS_ROLES: broker,controller)
5. Debezium entrypoint script is embedded in ConfigMap and mounted as executable
6. All pods show 2/2 READY after bootstrap (Istio sidecar injected in PERMISSIVE mode)
7. Debezium REST API returns both connector names
</verification>

<success_criteria>
- `bash scripts/k8s-cluster-up.sh` completes without error
- `kubectl get pods -n bankforge` shows all 7 infrastructure pods at 2/2 READY
- `kubectl exec -n bankforge deploy/debezium -- curl -sf http://localhost:8083/connectors` returns both connector names
- `kubectl exec -n bankforge account-db-0 -- psql -U account -d accountdb -c "SHOW wal_level;"` returns `logical`
- `kubectl get pods -n bankforge | grep -i zookeeper` returns no output (KRaft confirmed)
</success_criteria>

<output>
After completion, create `.planning/phases/03-service-mesh-auth/03-01-SUMMARY.md`
</output>
