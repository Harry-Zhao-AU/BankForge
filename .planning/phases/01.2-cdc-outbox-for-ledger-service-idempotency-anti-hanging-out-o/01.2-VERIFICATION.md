---
phase: 01.2-cdc-outbox-for-ledger-service-idempotency-anti-hanging-out-o
verified: 2026-04-16T00:00:00Z
status: human_needed
score: 27/28 must-haves verified
overrides_applied: 0
human_verification:
  - test: "Run TransferSagaIT end-to-end integration test suite"
    expected: "All 3 test scenarios (happy path, idempotency, DLT routing) pass"
    why_human: "Integration tests require Testcontainers (Docker/Podman running); cannot verify programmatically in this environment"
  - test: "Run LedgerEventListenerIT integration test suite"
    expected: "All 4 test scenarios pass with non-transactional KafkaTemplate sends"
    why_human: "Requires running containers (PostgreSQL + Kafka via Testcontainers)"
  - test: "Run HungTransferDetectorTest unit test suite"
    expected: "All 3 unit tests pass: shouldCancelHungPaymentProcessingTransfers, shouldMarkHungPostingAsFailed, shouldDoNothingWhenNoHungTransfers"
    why_human: "Test execution environment not available; test logic verified by code inspection"
---

# Phase 01.2: CDC Outbox for Ledger Service Verification Report

**Phase Goal:** Eliminate dual-write risk in ledger-service by implementing CDC outbox pattern; add idempotency guarantees; wire DLT for exhausted retries; propagate transactionId via OTel Baggage (D-15); detect and resolve hung transfers.
**Verified:** 2026-04-16
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | FAILED enum value exists in TransferState and compiles across all modules | VERIFIED | `TransferState.java` line 27: `FAILED` present after `CANCELLED` |
| 2 | Flyway V3 migration creates ledger_outbox_event table with Debezium-compatible column names | VERIFIED | `V3__add_ledger_outbox.sql`: `CREATE TABLE ledger_outbox_event` with `aggregatetype`, `aggregateid`, `type`, `payload JSONB`, `created_at TIMESTAMPTZ` |
| 3 | LedgerOutboxEvent JPA entity maps to ledger_outbox_event table with correct column names | VERIFIED | `LedgerOutboxEvent.java`: `@Table(name = "ledger_outbox_event")`, all 5 column fields present, `@JdbcTypeCode(SqlTypes.JSON)` on payload |
| 4 | Debezium connector JSON targets ledger-db:5432, watches public.ledger_outbox_event, routes to banking.transfer.confirmed | VERIFIED | `ledger-outbox-connector.json`: hostname=`ledger-db`, port=5432, table=`public.ledger_outbox_event`, route=`banking.transfer.confirmed` (literal) |
| 5 | Debezium connector propagates aggregateid as Kafka header transaction-id (D-15 Layer 1) | VERIFIED | `ledger-outbox-connector.json` line 26: `"transforms.outbox.table.fields.additional.placement": "aggregateid:header:transaction-id"` |
| 6 | register-connector.sh registers both account-service and ledger-service Debezium connectors idempotently | VERIFIED | Script contains PUT-if-exists/POST-if-new logic for both `bankforge-outbox-connector` and `bankforge-ledger-outbox-connector` |
| 7 | debezium-connect depends_on ledger-db in compose.yml | VERIFIED | `compose.yml` lines 141-142: `ledger-db: condition: service_healthy` under `debezium-connect.depends_on` |
| 8 | LedgerEventListener writes debit + credit + outbox row in a single JPA TX with no kafkaTemplate.send() | VERIFIED | `LedgerEventListener.java`: `@Transactional`, calls `ledgerOutboxEventRepository.save(outboxRow)` in same scope, zero `kafkaTemplate` references |
| 9 | Idempotency replay is a true no-op — no writes, no direct Kafka publish | VERIFIED | Lines 59-62: `if (existsByTransferId(transferId)) { log.info(...); return; }` — exits before any writes |
| 10 | DLT handler logs structured ERROR for exhausted-retry messages | VERIFIED | `DltHandler.java`: `log.error("DLT message received: topic={} partition={} offset={} key={} value={}",...)` |
| 11 | DeadLetterPublishingRecoverer routes to banking.transfer.events.DLT | VERIFIED | `KafkaConfig.java` line 54: `new TopicPartition("banking.transfer.events.DLT", r.partition())` |
| 12 | KafkaTransactionManager and EOSMode.V2 are removed from KafkaConfig | VERIFIED | `KafkaConfig.java`: no `KafkaTransactionManager` bean, no `EOSMode` reference; comment-only mention of EOS removal rationale |
| 13 | transaction-id-prefix and isolation-level removed from application.yml | VERIFIED | `application.yml`: producer block contains only `key-serializer` and `value-serializer`; consumer block has no `isolation-level` |
| 14 | OTel Baggage banking.transaction.id is set in LedgerEventListener.onTransferEvent | VERIFIED | Lines 65-68: `Baggage.current().toBuilder().put("banking.transaction.id", transferId.toString())` |
| 15 | OTel Baggage banking.transaction.id is set in PaymentService.initiateTransfer (D-15 Layer 2 both sites) | VERIFIED | `PaymentService.java` lines 134-137: `Baggage.current().toBuilder().put("banking.transaction.id", transfer.getId().toString())` |
| 16 | X-Transaction-Id header is returned in payment-service POST /transfer response | VERIFIED | `PaymentController.java` lines 53-55: `.header("X-Transaction-Id", response.transferId().toString())` |
| 17 | Hung PAYMENT_PROCESSING transfers older than 5 minutes are cancelled via TransferStateService.cancel() | VERIFIED | `HungTransferDetector.java`: `PAYMENT_PROCESSING_TIMEOUT_MINUTES = 5`, calls `transferStateService.cancel(transfer.getId(), "PAYMENT_PROCESSING_TIMEOUT")` |
| 18 | Hung POSTING transfers older than 15 minutes are set to FAILED with LEDGER_CONFIRM_TIMEOUT error message | VERIFIED | Lines 98-99: `transfer.setState(TransferState.FAILED)`, `transfer.setErrorMessage("LEDGER_CONFIRM_TIMEOUT -- manual review required")` |
| 19 | POSTING timeout does NOT publish reversal event or trigger compensation | VERIFIED | `resolveHungPosting()` method: only `setState(FAILED)` + `save()`, no `kafkaTemplate.send()` call |
| 20 | PAYMENT_PROCESSING timeout publishes banking.transfer.failed event | VERIFIED | `resolveHungPaymentProcessing()`: `kafkaTemplate.send(TRANSFER_FAILED_TOPIC, ...)` where `TRANSFER_FAILED_TOPIC = "banking.transfer.failed"` |
| 21 | @Scheduled job runs every 60 seconds | PARTIAL | Implementation uses `@Scheduled(fixedDelayString = "${payment.hung-transfer-detector.fixed-delay-ms:60000}")` — defaults to 60000ms, functionally equivalent to `fixedDelay = 60_000` but plan must_have artifact check (`contains: "@Scheduled(fixedDelay = 60_000)"`) does not match. Acceptable deviation (improves testability). |
| 22 | HungTransferDetectorTest covers both timeout scenarios | VERIFIED | Test class contains `shouldCancelHungPaymentProcessingTransfers`, `shouldMarkHungPostingAsFailed`, `shouldDoNothingWhenNoHungTransfers` |
| 23 | TransferSagaIT happy path: event received -> debit + credit + outbox row written | VERIFIED (code) | `TransferSagaIT.java` `shouldWriteEntriesAndOutboxOnTransferEvent` asserts 2 entries + 1 outbox row; needs human run |
| 24 | TransferSagaIT idempotency: duplicate event -> exactly 2 ledger entries, exactly 1 outbox row | VERIFIED (code) | `shouldBeIdempotentOnDuplicateDelivery` asserts `hasSize(2)` entries and `hasSize(1)` outbox rows |
| 25 | TransferSagaIT DLT routing: poison pill -> message lands in banking.transfer.events.DLT after retry exhaustion | VERIFIED (code) | `shouldRoutePoisonPillToDlt` uses raw KafkaConsumer, awaits DLT message with unique key per run |
| 26 | LedgerEventListenerIT updated to remove EOS transactional producer properties | VERIFIED | No `transaction-id-prefix`, no `isolation-level`, no `executeInTransaction` in updated IT file |
| 27 | LedgerEventListenerIT verifies outbox row is written | VERIFIED | Lines 100-104: `ledgerOutboxEventRepository.findAll()` filtered by `aggregateid`, `assertThat(outboxRows).hasSize(1)` |
| 28 | TransferRepository has findByStateAndCreatedAtBefore and findByStateAndUpdatedAtBefore | VERIFIED | `TransferRepository.java` lines 27 and 34: both derived query methods present |

**Score:** 27/28 truths verified (1 partial — functional equivalent of fixedDelay accepted)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `common/src/main/java/au/com/bankforge/common/enums/TransferState.java` | FAILED enum value | VERIFIED | `FAILED` present as last value after `CANCELLED` |
| `ledger-service/src/main/resources/db/migration/V3__add_ledger_outbox.sql` | Outbox table DDL | VERIFIED | All required columns present with correct types |
| `ledger-service/src/main/java/au/com/bankforge/ledger/entity/LedgerOutboxEvent.java` | JPA entity | VERIFIED | `@Table(name="ledger_outbox_event")`, `@JdbcTypeCode(SqlTypes.JSON)`, `@PrePersist` |
| `ledger-service/src/main/java/au/com/bankforge/ledger/repository/LedgerOutboxEventRepository.java` | Spring Data repository | VERIFIED | `extends JpaRepository<LedgerOutboxEvent, UUID>` |
| `infra/debezium/ledger-outbox-connector.json` | Debezium connector config | VERIFIED | All required fields present including transaction-id header propagation |
| `scripts/register-connector.sh` | Idempotent connector registration | VERIFIED | Registers both account and ledger connectors with PUT/POST logic |
| `ledger-service/src/main/java/au/com/bankforge/ledger/kafka/LedgerEventListener.java` | Outbox-based listener | VERIFIED | No `kafkaTemplate.send`, has `ledgerOutboxEventRepository.save` |
| `ledger-service/src/main/java/au/com/bankforge/ledger/config/KafkaConfig.java` | Refactored KafkaConfig | VERIFIED | `DeadLetterPublishingRecoverer` present, no `KafkaTransactionManager` |
| `ledger-service/src/main/java/au/com/bankforge/ledger/kafka/DltHandler.java` | DLT consumer | VERIFIED | `@KafkaListener(topics = "banking.transfer.events.DLT", groupId = "ledger-service-dlt")`, read-only |
| `ledger-service/src/main/resources/application.yml` | Cleaned config | VERIFIED | No `transaction-id-prefix`, no `isolation-level` |
| `payment-service/src/main/java/au/com/bankforge/payment/controller/PaymentController.java` | X-Transaction-Id header | VERIFIED | `.header("X-Transaction-Id", response.transferId().toString())` |
| `payment-service/src/main/java/au/com/bankforge/payment/service/PaymentService.java` | OTel Baggage | VERIFIED | `Baggage.current().toBuilder().put("banking.transaction.id", ...)` with scope wrap |
| `payment-service/src/main/java/au/com/bankforge/payment/detector/HungTransferDetector.java` | Scheduled detector | VERIFIED | Two timeout rules, fixedDelayString=60000ms |
| `payment-service/src/test/java/au/com/bankforge/payment/detector/HungTransferDetectorTest.java` | Unit tests | VERIFIED | 3 test methods covering all behavioral rules |
| `payment-service/src/main/java/au/com/bankforge/payment/repository/TransferRepository.java` | Repository query methods | VERIFIED | Both `findByStateAndCreatedAtBefore` and `findByStateAndUpdatedAtBefore` present |
| `payment-service/src/main/java/au/com/bankforge/payment/PaymentServiceApplication.java` | @EnableScheduling | VERIFIED | `@EnableScheduling` annotation present |
| `ledger-service/src/test/java/au/com/bankforge/ledger/saga/TransferSagaIT.java` | Integration test | VERIFIED (code) | 3 scenarios present, no EOS properties, non-transactional sends |
| `ledger-service/src/test/java/au/com/bankforge/ledger/kafka/LedgerEventListenerIT.java` | Updated IT | VERIFIED | EOS properties removed, outbox row assertion added |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `LedgerEventListener.java` | `LedgerOutboxEventRepository` | `ledgerOutboxEventRepository.save(outboxRow)` in same `@Transactional` | WIRED | Line 100: `ledgerOutboxEventRepository.save(outboxRow)` inside `@Transactional` method |
| `KafkaConfig.java` | `DltHandler.java` | `DeadLetterPublishingRecoverer` routes to `banking.transfer.events.DLT` | WIRED | `KafkaConfig` creates recoverer routing to DLT; `DltHandler` consumes `banking.transfer.events.DLT` |
| `LedgerEventListener.java` | OTel traces | `Baggage.current().toBuilder().put("banking.transaction.id", ...)` | WIRED | Lines 65-68 in listener |
| `PaymentService.java` | OTel traces | `Baggage.current().toBuilder().put("banking.transaction.id", ...)` | WIRED | Lines 134-137 in PaymentService |
| `infra/debezium/ledger-outbox-connector.json` | `compose.yml debezium-connect` | `database.hostname = ledger-db` | WIRED | Compose service `ledger-db` matches connector hostname |
| `infra/debezium/ledger-outbox-connector.json` | Kafka record headers | `transforms.outbox.table.fields.additional.placement: aggregateid:header:transaction-id` | WIRED | Present in connector JSON |
| `entity/LedgerOutboxEvent.java` | `db/migration/V3__add_ledger_outbox.sql` | `@Table(name = "ledger_outbox_event")` matches `CREATE TABLE` | WIRED | Both use `ledger_outbox_event` |
| `HungTransferDetector.java` | `TransferStateService.cancel()` | `transferStateService.cancel(t.getId(), "PAYMENT_PROCESSING_TIMEOUT")` | WIRED | Line 74 in `resolveHungPaymentProcessing()` |
| `HungTransferDetector.java` | `TransferRepository` | `findByStateAndCreatedAtBefore` + `findByStateAndUpdatedAtBefore` | WIRED | Both methods called in `HungTransferDetector` |
| `HungTransferDetector.java` | `Transfer.state` | Direct `setState(TransferState.FAILED)` for POSTING timeout | WIRED | Line 98: `transfer.setState(TransferState.FAILED)` |
| `TransferSagaIT.java` | `LedgerOutboxEventRepository` | Asserts outbox row count via repository | WIRED | `ledgerOutboxEventRepository` autowired and queried in all 3 scenarios |
| `TransferSagaIT.java` | `banking.transfer.events.DLT` | Raw KafkaConsumer subscribed to DLT topic | WIRED | `consumer.subscribe(Collections.singletonList("banking.transfer.events.DLT"))` |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| `LedgerEventListener.java` | `entries` (debit/credit) | `ledgerEntryRepository.save()` from Kafka event payload | Yes — writes from event fields | FLOWING |
| `LedgerEventListener.java` | `outboxRow` | Built from `transferId`, `aggregatetype`, `type`, `payload` | Yes — JPA save persists to DB, Debezium reads WAL | FLOWING |
| `HungTransferDetector.java` | `hung` (PAYMENT_PROCESSING) | `transferRepository.findByStateAndCreatedAtBefore()` — JPA derived query against DB | Yes — reads real Transfer rows | FLOWING |
| `HungTransferDetector.java` | `hung` (POSTING) | `transferRepository.findByStateAndUpdatedAtBefore()` — JPA derived query against DB | Yes — reads real Transfer rows | FLOWING |

### Behavioral Spot-Checks

Step 7b: SKIPPED for integration test classes (require running containers). Production code spot-checks below:

| Behavior | Check | Result | Status |
|----------|-------|--------|--------|
| LedgerEventListener has no kafkaTemplate.send() | `grep -n "kafkaTemplate" LedgerEventListener.java` | No matches in production code | PASS |
| KafkaConfig has no KafkaTransactionManager | Inspected file | No `KafkaTransactionManager` bean or import | PASS |
| application.yml has no transaction-id-prefix | Inspected file | Producer block: only key-serializer and value-serializer | PASS |
| DltHandler has no send/publish calls | Inspected DltHandler.java | No `kafkaTemplate`, no `send` in file | PASS |
| HungTransferDetector POSTING path has no kafkaTemplate.send | Inspected `resolveHungPosting()` | Method contains only `setState`, `setErrorMessage`, `save()` | PASS |
| compose.yml debezium-connect depends on ledger-db | `grep -n ledger-db compose.yml` | Line 141: `ledger-db: condition: service_healthy` under debezium-connect | PASS |

### Requirements Coverage

| Requirement | Source Plans | Description | Status | Evidence |
|-------------|-------------|-------------|--------|---------|
| CORE-04 | 01.2-01, 01.2-02, 01.2-04 | System records every transfer as a double-entry ledger pair | SATISFIED | `LedgerEventListener` writes DEBIT + CREDIT entries in `@Transactional`; `TransferSagaIT` verifies `hasSize(2)` with both entry types |
| TXNS-02 | 01.2-01, 01.2-02, 01.2-04 | Outbox rows captured by Debezium CDC without dual-write | SATISFIED | `LedgerEventListener` no longer calls `kafkaTemplate.send()`; writes outbox row only; Debezium connector configured to publish to `banking.transfer.confirmed` |
| TXNS-03 | 01.2-02, 01.2-03, 01.2-04 | Downstream services consume Kafka events via Saga choreography | SATISFIED | `LedgerEventListener` is a Kafka consumer in a choreography Saga; DLT handler captures failures; `HungTransferDetector` manages Saga timeouts |

**Orphaned requirements from Phase 1.1 roadmap not in scope for Phase 01.2:**

| Requirement | Phase 1.1 Item | Why Not in Phase 01.2 |
|-------------|---------------|----------------------|
| CORE-05 | Notification-service consuming Kafka events | Not claimed in any 01.2 plan `requirements:` field |
| AUBN-01 | BSB format validation | Not claimed in any 01.2 plan `requirements:` field |
| AUBN-02 | AUSTRAC threshold audit logging | Not claimed in any 01.2 plan `requirements:` field |

These are not gaps in Phase 01.2 — they are addressed by other Phase 1.1 sub-phases. STATE.md confirms Phase 1.1 is marked complete (3/3 plans).

### Anti-Patterns Found

| File | Pattern | Severity | Impact |
|------|---------|----------|--------|
| `HungTransferDetector.java` | `@Scheduled(fixedDelayString = ...)` instead of `@Scheduled(fixedDelay = 60_000)` | Info | Functionally equivalent (defaults to 60000ms). `fixedDelayString` is an intentional deviation that enables testability via property override (`payment.hung-transfer-detector.fixed-delay-ms`). Not a stub or regression. |

No blockers or warnings found. No TODO/FIXME/placeholder comments, no empty implementations, no hardcoded empty data structures in rendering paths.

### Human Verification Required

#### 1. HungTransferDetectorTest Suite

**Test:** `cd C:/SideProject/BankForge && "C:/Program Files/JetBrains/IntelliJ IDEA Community Edition 2025.2.4/plugins/maven/lib/maven3/bin/mvn" -pl payment-service test -Dtest=HungTransferDetectorTest`
**Expected:** BUILD SUCCESS — 3 tests pass: `shouldCancelHungPaymentProcessingTransfers`, `shouldMarkHungPostingAsFailed`, `shouldDoNothingWhenNoHungTransfers`
**Why human:** Test execution environment (Maven + JVM) required; code inspection confirms correct logic but CI/test runner needed

#### 2. TransferSagaIT Integration Test

**Test:** `cd C:/SideProject/BankForge && "C:/Program Files/JetBrains/IntelliJ IDEA Community Edition 2025.2.4/plugins/maven/lib/maven3/bin/mvn" -pl ledger-service test -Dtest=TransferSagaIT`
**Expected:** BUILD SUCCESS — 3 tests pass: happy path (entries + outbox row), idempotency (no duplicates), DLT routing (poison pill in DLT topic after ~7s retry exhaustion)
**Why human:** Requires Testcontainers (PostgreSQL + Kafka containers); cannot run programmatically in static analysis mode

#### 3. LedgerEventListenerIT Integration Test

**Test:** `cd C:/SideProject/BankForge && "C:/Program Files/JetBrains/IntelliJ IDEA Community Edition 2025.2.4/plugins/maven/lib/maven3/bin/mvn" -pl ledger-service test -Dtest=LedgerEventListenerIT`
**Expected:** BUILD SUCCESS — 4 tests pass: `shouldWriteDebitAndCreditOnTransferEvent` (including outbox assertion), `shouldBeIdempotentOnDuplicateDelivery`, `shouldRollbackBothEntriesWhenSecondSaveFails`, `shouldNotDuplicateEntriesOnRetryAfterKafkaFailure`
**Why human:** Requires Testcontainers; non-transactional KafkaTemplate must match application config (verified in code)

#### 4. Full Compile Verification

**Test:** `"C:/Program Files/JetBrains/IntelliJ IDEA Community Edition 2025.2.4/plugins/maven/lib/maven3/bin/mvn" -pl common,ledger-service,payment-service compile -Dmaven.test.skip=true`
**Expected:** BUILD SUCCESS — all 3 modules compile cleanly
**Why human:** Maven execution required; SUMMARY.md documents this passed but cannot re-verify without running Maven

### Gaps Summary

No gaps identified. All must-haves from all 4 plan frontmatter definitions are satisfied or have acceptable deviations. The one partial item (`@Scheduled` annotation style) is an intentional improvement over the plan spec and has no behavioral impact.

The phase goal is **achieved**:
- Dual-write risk eliminated: `LedgerEventListener` writes outbox row only; no `kafkaTemplate.send()`
- CDC pipeline configured: Debezium connector targets `ledger-db`, routes to `banking.transfer.confirmed`, propagates `transaction-id` header
- Idempotency: `existsByTransferId` guard is a true no-op on replay
- DLT wired: `DeadLetterPublishingRecoverer` + `DltHandler` capture exhausted-retry messages
- OTel Baggage propagated at both sites: `PaymentService.initiateTransfer` and `LedgerEventListener.onTransferEvent`
- X-Transaction-Id HTTP response header added
- Hung-transfer detection implemented with correct per-state rules (PAYMENT_PROCESSING cancel, POSTING FAILED with no reversal)
- Test coverage: unit tests (HungTransferDetectorTest), integration tests (TransferSagaIT + updated LedgerEventListenerIT)

Status is `human_needed` solely because the test suites require running container infrastructure (Testcontainers) and Maven execution to verify at runtime. All static code analysis confirms full implementation.

---

_Verified: 2026-04-16_
_Verifier: Claude (gsd-verifier)_
